using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.ConstrainedExecution;
using System.Text;
using System.Threading.Tasks;

namespace Example_String
{
    internal class Strings
    {
        
        static void Main(String[] args)
        {
            /*String firstName = "Nirmala";
            String lastName = "Bhatt";
           var fullName= firstName + lastName;
             var formattedString = string.Format("my name is {0} {1}", firstName, lastName);
             Console.WriteLine(formattedString);
             string[] names = new string[3] { "niru","saita","bhuwan" };
             var displyNames = string.Join(",", names);
             Console.WriteLine(displyNames);

              @operator op = new @operator();
             op.operatorExample();
            */
            
            ifElse i = new ifElse();
             i.ifElsecase();
           

        }

    }
}
